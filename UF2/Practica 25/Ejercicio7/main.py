from module import functions as fc  # Importamos el modulo con las funciones y un alias.
print(fc.randomarray([1, 2, 3, 5, 13, 25, 9]))  # Llamamos a la funcion y le pasamos los parametros necesarios.
