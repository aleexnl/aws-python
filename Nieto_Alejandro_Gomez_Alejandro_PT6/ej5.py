# Ponemos la variable suma a 0
suma=0
# Y decimos que i si esta en el rango de 101(por que incluimos el 100) a la suma le sumaremos el valor que tiene suma mas el valor que tiene i
for i in range (101)    :
    suma=suma+i
# Cuando i ya no este en el rango, printaremos la variable suma que es donde estara el resultado
print(suma)
